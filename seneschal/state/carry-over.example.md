# Carry-over — the assistant's running "open loops" (SEED / example)

> **This is the tracked seed.** The live carry-over is `state/carry-over.md` (gitignored, local-first).
> It tracks the assistant's own open loops — held drafts awaiting approval, unanswered calendar invites,
> follow-ups it promised, and Active/Carrying-Over flags worth keeping in view. Rebuilt from current
> state each run (resolved items drop off; new ones appear). If the live file is missing (fresh
> checkout), copy this seed to `state/carry-over.md`.
>
> For the mechanics (where it lives, the held-approval loop, the pending-approvals schema), see the
> tracked doc `../references/memory.md`.

## Open follow-ups (the assistant's self-tasks)

<!-- Entry format (one bullet per open loop / dated follow-up):
- **YYYY-MM-DD (Mode/context):** what's open, why, and what closes it. Mark RESOLVED inline when done
  (kept briefly for continuity), then drop on the next rebuild.
-->
