# Context digest (SEED / example)

> **This is the tracked seed.** The live digest is `state/context-digest.md` (gitignored, local-first).
> The assistant's **Dream mode** (nightly wind-down) **overwrites** the live file with a condensed view of
> the day so the next morning's **Brief** can orient cheaply without re-querying everything. If the live
> file is missing (fresh checkout), copy this seed to `state/context-digest.md` — the next Dream
> regenerates it. The active store (Run Log + carry-over) is the system of record; this is a convenience
> cache.

**Last consolidated:** _(none yet — seed)_

## Yesterday in one breath
_What actually happened — the few things worth remembering._

## Open loops
_Unresolved items carried forward (held drafts awaiting approval, follow-ups the assistant promised)._

## Queued for today
_What today has on it: due tasks, first events, anything time-boxed._

## Active flags
_Important Flags still Active / Carrying Over, highest importance first._

## Reminders pending
_Upcoming entries from `reminders.json` (not yet fired), plus on-deck deadline-watch items._

## Brief pre-stage (Dream)
_Snapshot: (none yet — seed)._
> Dream pre-stages the morning **Brief**'s read-only Notion inputs here so the Brief can skip its full
> 4-read batch and only delta-check live (`../references/notion-rate-limits.md` — Dream digest pre-staging).
> **Staleness caveat:** this is snapshotted ~22:00 the night before, so the Brief still runs a light
> morning delta query (since-stamp completions / same-day-new tasks / changed flags & projects) on top of
> it — a warm base, not the final word. Each item lists its name + `id`/`url` so the Brief can cite + diff.

- **Tasks due/overdue (today + tomorrow):** _e.g._ `Ship the quarterly report` (High, due today) — <task-url>
- **Active / Carrying-Over flags:** _e.g._ `🚨 Rent due Fri` (Critical, Active) — <flag-url>
- **In-Progress projects:** _e.g._ `Local-first migration` — <project-url>
