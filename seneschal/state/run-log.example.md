# Run Log — run history (SEED / example)

> **This is the tracked seed.** The live run history is `state/run-log.md` (gitignored, local-first).
> A substantive run appends a dated entry here as a convenience mirror; the **🧭 Run Log** database in
> the active store (`collection://00000000-0000-0000-0000-000000000008`) is the **system of record**.
> If the live file is missing (fresh checkout), copy this seed to `state/run-log.md`.
>
> For the write protocol (when to write, incremental phase logging, finalizing counts), see the tracked
> doc `../references/memory.md`.

## Run history (incremental — newest appended at the end)

<!-- Entry format (one bullet per substantive run):
- **YYYY-MM-DD (Mode/Slot):** one-tight-paragraph trace of what happened — what was read/written, what
  was surfaced or held, carries into tomorrow, any issues. End with the row id:
  🧭 Run Log row `<page-id>`.
-->
